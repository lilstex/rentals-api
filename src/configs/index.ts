import env from "./env";
import Constants from "./constants";
import Swagger from "./swagger";
import DatabaseManager from "./db";

const modules = {
  env,
  Constants,
  Swagger,
  DatabaseManager,
};

export = modules;